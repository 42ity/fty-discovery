<!--
Installation model

    1. by path & name
    path - absolute path to the file
    name - file name

    2. type
    * systemd-tmpfiles  - install $(project.name) to /usr/lib/systemd/tmpfiles.d/
     -->

<install>
    <item path = "/etc/sudoers.d/" name = "fty_20_discovery" />
</install>
